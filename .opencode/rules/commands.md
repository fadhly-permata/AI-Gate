# COMMANDS
One need, one command file. Multi-action via param system, not many file.
Example: `/skill get|search|delete|help`.
Add help alias: `help` / `info` / `information` / `?`. List supported param + aim.
